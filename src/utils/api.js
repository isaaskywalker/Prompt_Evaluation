import axios from 'axios';

export const fetchOpenAIResponse = async (prompt, apiKey, hyperparameters) => {
    // OpenAI API 호출 로직
};

export const fetchClaudeResponse = async (prompt, apiKey, hyperparameters) => {
    // Claude API 호출 로직
};

export const fetchGeminiResponse = async (prompt, apiKey, hyperparameters) => {
    // Gemini API 호출 로직
};
